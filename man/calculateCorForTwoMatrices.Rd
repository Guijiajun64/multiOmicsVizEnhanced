% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculateCorForTwoMatrices.R
\name{calculateCorForTwoMatrices}
\alias{calculateCorForTwoMatrices}
\title{Calculate Spearman Correlation Between Two Omics Matrices}
\usage{
calculateCorForTwoMatrices(matrix1, matrix2, fdr)
}
\arguments{
\item{matrix1}{A matrix, data.frame, or `SummarizedExperiment` object. 
Rows should be genes and columns samples.}

\item{matrix2}{A matrix, data.frame, or `SummarizedExperiment` object 
with matching samples.}

\item{fdr}{A numeric value indicating the FDR threshold.}
}
\value{
A correlation matrix where significant correlations are encoded 
  as +1 (positive), -1 (negative), and 0 (non-significant).
}
\description{
This function computes the gene-by-gene Spearman correlation between two 
omics matrices and applies FDR correction to identify significant 
correlations. It supports both matrix/data.frame inputs and 
`SummarizedExperiment` objects.
}
\examples{
\dontrun{
set.seed(1)
m1 <- matrix(rnorm(200), nrow = 20)
m2 <- matrix(rnorm(200), nrow = 20)
calculateCorForTwoMatrices(m1, m2, fdr = 0.05)
}


}
